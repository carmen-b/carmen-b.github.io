---
layout: layout.njk
permalink: /portfolio/index.html
title: Portfolio | Carmen Balaban
---
<section class="section">
  <div class="container">
    <div class="section-header centered">
      <h1>Portfolio</h1>
      <p>A curated selection of branding, print, stationery, and illustration projects.</p>
    </div>

    <nav aria-label="Portfolio filters">
      <ul class="filter-list">
        <li><a href="/portfolio/" class="is-active">All</a></li>
        {% for category in collections.portfolioCategories %}
        <li><a href="/portfolio/{{ category.slug }}/">{{ category.label }}</a></li>
        {% endfor %}
      </ul>
    </nav>

    <div class="portfolio-grid">
      {% for item in collections.portfolioItems %}
      <article class="portfolio-card">
        <a href="/portfolio/{{ item.data.category }}/{{ item.data.slug }}.html" title="{{ item.data.title }}">
          <img src="{{ item.data.category }}/{{ item.data.slug }}-thumb.jpg" alt="{{ item.data.title }} thumbnail">
          <div class="portfolio-meta">
            <h2>{{ item.data.title }}</h2>
            <p>{{ item.data.categoryLabel }}</p>
          </div>
        </a>
      </article>
      {% endfor %}
    </div>
  </div>
</section>
