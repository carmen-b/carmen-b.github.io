<%
    if (current.path.length==3 ) { 
        if (current.source == 'index') {
            partName='_category.ejs'; 
        } else {
            partName='_item.ejs'; 
        }
    } else { 
        partName='_all.ejs';
    }
%>
<%- partial('_helpers') %>
<%- partial(partName) %>